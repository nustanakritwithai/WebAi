# WebAi UI Development Plan

สถานะ: แผนงานก่อนลงมือพัฒนา — ปรับตามเลย์เอาต์แอปสามส่วนที่ผู้ใช้ระบุ

## เป้าหมาย

ทำให้ WebAi เป็นหน้าทำงานแบบแอปตามแนว Codex ที่ผู้ใช้ต้องการ: เมนูและรายการงานอยู่ซ้าย แชทอยู่ตรงกลาง และ Workspace อยู่ขวา ผู้ใช้สนทนากับ Agent ไปพร้อมกับดูไฟล์หรือ Preview ได้ในหน้าจอเดียว โดยคงชื่อและเอกลักษณ์ WebAi

นี่คือข้อกำหนดการออกแบบ WebAi ตามคำขอ ไม่ใช่ข้ออ้างว่าเป็นสำเนาหน้าตา Codex รุ่นปัจจุบัน

## หลักการออกแบบ

- ให้ Task เป็นศูนย์กลางของหน้าจอ ไม่ให้แชทเป็นที่เก็บโค้ดหลัก
- แสดงแผน, สถานะ, รายชื่อไฟล์, Preview และผลตรวจในลำดับเดียวกับการทำงานของ Agent
- ใช้ข้อความสั้น ปุ่มหลักเด่น และมีสถานะที่อ่านได้โดยไม่ต้องเปิด Console
- การเปลี่ยน task ต้องเห็นชัด และไม่ลบไฟล์ของ task เดิมโดยอัตโนมัติ
- จอเล็กต้องใช้งานได้ด้วยนิ้วเดียว ปุ่มกดง่าย และไม่บังคับเลื่อนแนวนอน

## โครงสร้างหน้าจอเป้าหมาย

```text
┌────────────────┬────────────────────────┬────────────────────────┐
│ WebAi          │ ชื่องาน · สถานะ Agent   │ Workspace         ขยาย │
│ + งานใหม่      ├────────────────────────┼────────────────────────┤
│ โปรเจกต์       │ ข้อความผู้ใช้          │ Files Preview Diff Tests│
│ งานล่าสุด      │ คำตอบ / แผนของ Agent   ├────────────────────────┤
│ • งานปัจจุบัน  │ กิจกรรมเขียนไฟล์       │ tree │ แท็บไฟล์          │
│ • งานก่อนหน้า  │ ปุ่มอนุมัติ / ลองใหม่   │      │ editor / preview  │
│                │                        │      │                   │
│ ค้นหางาน       ├────────────────────────┤      │                   │
│ การตั้งค่า     │ พิมพ์ข้อความ…     ส่ง  │      │                   │
└────────────────┴────────────────────────┴────────────────────────┘
```

### ซ้าย: เมนูและรายการงาน

- กว้างเริ่มต้น 240px พับเหลือแถบไอคอนหรือซ่อนได้
- มีชื่อ WebAi, งานใหม่, โปรเจกต์, ค้นหางาน และรายการงานล่าสุดพร้อมสถานะ
- งานที่เลือกมี highlight ชัดเจน; เลือกงานแล้วแชทและ Workspace เปลี่ยนพร้อมกัน
- การตั้งค่าและการเชื่อมต่ออยู่ด้านล่าง; หน้าเครื่องมือเสริมเข้าผ่านเมนู ไม่แย่งพื้นที่งานหลัก

### กลาง: แชทและการควบคุม Agent

- แชทเป็นพื้นที่หลักตลอดอายุ task มีประวัติข้อความต่อเนื่องและเลื่อนภายในแผง
- หัวแชทแสดงชื่องานกับสถานะสั้น ๆ; ข้อมูล model และรายละเอียด runtime เปิดดูเพิ่มเติมได้
- Composer ยึดด้านล่างของแผง มีข้อความหลายบรรทัด ปุ่มส่ง และปุ่มหยุดเมื่อระบบรองรับการยกเลิกจริง
- Desktop: Enter ส่ง, Shift+Enter ขึ้นบรรทัดใหม่; มือถือใช้ปุ่มส่งและ Enter ขึ้นบรรทัดใหม่; ไม่ส่งระหว่าง IME composition
- แผนและการอนุมัติอยู่ในข้อความ Agent กิจกรรมเครื่องมือพับได้ เช่น “บันทึก 3 ไฟล์” พร้อมลิงก์เปิดไฟล์ด้านขวา
- เมื่อผู้ใช้เลื่อนอ่านย้อนหลัง ไม่บังคับเลื่อนตามข้อความใหม่ แสดงปุ่มกลับไปข้อความล่าสุด
- โค้ดหรือเอกสารที่เป็นผลลัพธ์งานเปิดเป็นไฟล์ใน Workspace; ข้อความสนทนายังอธิบายหรือยกตัวอย่างโค้ดสั้น ๆ ได้

### ขวา: Workspace

- เปิดคู่กับแชท มีแท็บ Files, Preview, Diff และ Tests; PLAN.md เปิดเป็นเอกสารจาก Files ได้
- Files มี tree พับได้และแท็บไฟล์ เปิด อ่าน แก้ บันทึก และแสดงสถานะยังไม่บันทึก
- กดชื่อไฟล์จากแชทแล้วเปิดไฟล์นั้นโดยรักษาข้อความที่กำลังพิมพ์
- Preview ใช้ revision ที่บันทึกแล้ว มีปุ่มรันและเลือกขนาดจำลอง Desktop/Tablet/Mobile
- Diff และ Tests แสดงการเปลี่ยนแปลงกับผลตรวจของงานเดียวกัน รายละเอียด Inspector อยู่ในแผงนี้และพับได้
- รองรับลากแบ่งสัดส่วนกับแชท พับ Workspace และขยายเต็มพื้นที่ทำงาน พร้อมปุ่มกลับ
- เมื่อยังไม่มีไฟล์ แสดง empty state สั้น ๆ; หากบันทึกไม่สำเร็จให้แสดง error และเก็บเนื้อหาแก้ไขไว้

### รูปลักษณ์และโครงหน้าต่าง

ใช้พื้นผิวเรียบ โทนกลาง เส้นแบ่งบาง สีเน้นหนึ่งชุด และตัวอักษรอ่านง่าย ลดการ์ดซ้อน เงาหนัก และแบนเนอร์ขนาดใหญ่ โครงแอปสูงเต็ม viewport (`100dvh` พร้อม fallback) แต่ละแผงเลื่อนของตัวเอง ข้อความแชทมีความกว้างอ่านสบายและ code/editor เลื่อนแนวนอนภายในกรอบได้

## Flow หลักของผู้ใช้

`New Task → พิมพ์เป้าหมาย → Plan → Approve → เขียนไฟล์ใน task folder → Preview → Verify → Done`

การแก้งานต่อใช้:

`Continue Task → อ่านไฟล์และบริบทเดิม → Plan patch → Approve → เขียน revision ใหม่ → Preview → Verify`

หน้าจอต้องแสดงปุ่มถัดไปเพียงปุ่มที่เหมาะกับสถานะปัจจุบัน เช่น `Approve`, `Run Preview`, `Verify` หรือ `Fix Again` และต้องปิดปุ่มที่ใช้ไม่ได้

## Responsive plan

### Desktop: ตั้งแต่ 1,200px

- แสดงสามส่วนพร้อมกัน: ซ้าย 240px กลางประมาณ 45% และขวาประมาณ 55% ของพื้นที่ที่เหลือ
- แชทกว้างขั้นต่ำประมาณ 360px และ Workspace 400px; จำกัดการลากไม่ให้แผงเล็กจนใช้งานไม่ได้
- แชทและ Workspace เลื่อนแยกกัน Composer อยู่ใต้แชทเสมอ
- จดจำสัดส่วนและแผงที่พับ แต่ปรับใหม่เมื่อหน้าจอเล็กลง

### Tablet: 768–1,199px

- เมนูซ้ายเปิดเป็น drawer จากปุ่มเมนู
- แสดงแชทกับ Workspace คู่กันเมื่อพื้นที่เพียงพอ; หากต่ำกว่า minimum width ให้สลับ Chat / Workspace แบบเต็มแผง
- มีปุ่มขยาย Workspace แล้วกลับสู่แชทโดยไม่สูญเสีย draft หรือ scroll position

### Mobile: ต่ำกว่า 768px

- แสดงหนึ่งแผงเต็มจอ โดยเริ่มที่แชท มีตัวสลับ Chat / Workspace ใน header และปุ่มเมนูเปิด drawer ซ้าย
- ไม่ย่อสามคอลัมน์ลงจอมือถือ และไม่ใช้ bottom navigation ห้ารายการซ้ำกับ composer
- Composer อยู่เหนือคีย์บอร์ดและ safe area; ทดสอบเมื่อเปิด/ปิดคีย์บอร์ดจริง ไม่ให้บังข้อความหรือปุ่มส่ง
- Workspace มี Files / Preview / Diff / Tests แบบเลื่อนแท็บภายในแถบได้ Files ใช้ tree แล้วแตะเปิด editor เต็มแผงพร้อมปุ่มย้อนกลับ
- กด artifact ในแชทจะสลับเข้า Workspace; กลับแชทแล้วยังคง draft และตำแหน่งอ่านเดิม
- Preview กว้างตามพื้นที่ใช้ได้ ขนาดจำลองที่ใหญ่กว่าจอเลื่อนภายในกรอบ ไม่ทำให้ทั้งหน้าไหลออกด้านข้าง
- ปุ่มหลักสูงอย่างน้อย 44px, ข้อความไม่เล็กเกินไป และไม่ใช้ hover เป็นเงื่อนไขการใช้งาน
- Drawer, modal และ command palette ต้องเลื่อนในแนวตั้งได้และปิดได้ด้วยปุ่มที่เข้าถึงง่าย

## สถานะและข้อความ

ใช้สถานะกลางชุดเดียวทั่ว UI:

`idle`, `planning`, `awaiting_approval`, `executing`, `awaiting_preview`, `verifying`, `completed`, `repairing`, `failed`, `cancelled`, `interrupted`

ทุกสถานะต้องมีข้อความสั้นภาษาไทยและ action ถัดไปที่แน่นอน ห้ามแสดง `completed` เพียงเพราะโมเดลตอบกลับ ต้องมี file revision และ verification evidence ที่ตรงกับ task ปัจจุบัน

## Accessibility และคุณภาพ

- ทุกปุ่มและ input มี accessible name
- ลำดับ focus ใช้งานได้ด้วยคีย์บอร์ด
- สีสถานะต้องมีข้อความหรือไอคอนร่วมด้วย ไม่ใช้สีอย่างเดียว
- รองรับ `prefers-reduced-motion`
- ตรวจ contrast, zoom 200%, หน้าจอแนวตั้ง และ keyboard navigation
- ไม่ใช้โค้ดตัวอย่างจากโมเดลเป็น HTML ของหน้า UI โดยตรง

## ลำดับการพัฒนา

1. **UI audit และ wireframe** — ตรวจ index.html, main-ui-v2.js/css และ styles.css รวมถึง element ID ที่ Agent ใช้อยู่ วางแบบสามแผงและมือถือก่อนแก้จริง
2. **App shell** — จัดซ้าย/กลาง/ขวาเต็ม viewport, scroll แยก, resize/collapse และ drawer โดยรักษา hook เดิมระหว่างย้าย
3. **Chat** — ย้าย composer ลงล่าง เพิ่มประวัติข้อความและกิจกรรม Agent ที่อ้าง task เดียวกัน รวม listener ส่งคำสั่งให้มีจุดเดียว
4. **Workspace** — เชื่อมลิงก์ artifact → แท็บไฟล์, tree/editor, Preview/Diff/Tests และป้องกันเสีย draft เมื่อเปลี่ยนงาน
5. **Mobile pass** — ทดสอบ 360px, 390px, 768px, 1024px, 1280px และ 1440px รวมการหมุนจอและคีย์บอร์ดมือถือ
6. **Interaction และ visual QA** — ตรวจสร้างงาน/แก้ต่อ/อนุมัติ/เปิดไฟล์/บันทึก/Preview/Verify/reload ด้วย browser จริง แล้วบันทึกผลที่ตรวจได้

งาน UI ใช้ API และสถานะจากระบบ Agent ไม่สร้างวงจร Agent อีกชุด ปุ่มที่ backend ยังไม่รองรับต้องไม่แสดงเป็นความสามารถที่ใช้ได้จริง

## เกณฑ์รับงาน

- ผู้ใช้สร้าง task ใหม่และเห็นโฟลเดอร์ task ทันที
- Desktop เห็นเมนูซ้าย แชทกลาง Workspace ขวาพร้อมกัน; ไม่มีส่วนหลักถูกดันไปเป็นการ์ดต่อยาวใต้แชท
- พับ/ขยาย/ปรับสัดส่วนแผงแล้ว composer และปุ่มสำคัญยังเข้าถึงได้
- คลิกไฟล์ในข้อความแล้วเปิดไฟล์ถูกต้องด้านขวา โดยไม่รีเซ็ตแชทหรือข้อความร่าง
- มือถือสลับ Chat / Workspace และเข้าออกเมนูได้ด้วย touch; ปุ่มส่งไม่ถูกคีย์บอร์ดหรือ safe area บัง
- ผู้ใช้แก้ task เดิมได้โดยไม่สร้าง task ซ้ำโดยไม่ตั้งใจ
- Plan, Files, Preview และ Verify แสดงข้อมูลจาก task/revision เดียวกัน
- ไม่มีโค้ดดิบจาก Agent หลุดมาแทนไฟล์ใน Workspace
- ใช้งานได้ที่ความกว้าง 360px โดยไม่ต้องซูมและไม่เกิด horizontal overflow
- ปุ่ม action หลักกดได้ด้วยนิ้วและมี feedback หลังแตะ
- รีเฟรชหน้าแล้ว task ปัจจุบันและไฟล์ยังกลับมาได้
- ตรวจ accessibility พื้นฐานและไม่มี error ใน Console ระหว่าง flow หลัก
- Drawer ปิดด้วย Escape/ปุ่มปิดได้ จัดการ focus ภายในและคืน focus ให้ปุ่มเปิด; resize handle ใช้คีย์บอร์ดได้
- ทดสอบชื่อไฟล์ยาว ข้อความยาว task ว่าง loading/error และเนื้อหายังไม่บันทึก ไม่ทดสอบเฉพาะหน้าที่มีข้อมูลครบ

## สิ่งที่จะยังไม่ทำในรอบ UI แรก

- เปลี่ยนสีหรือโลโก้โดยไม่มีผลต่อ flow
- เพิ่ม animation ที่ไม่ช่วยให้เข้าใจสถานะ
- เพิ่มหน้าต่างแชทซ้อนหลายชั้น
- เพิ่มการรัน shell หรือเขียนไฟล์ในเครื่องจริง
