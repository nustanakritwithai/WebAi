# แผนพัฒนา Agent: รักษางานเดิม ทำต่อจากข้อสรุป และทำตาม TODO

สถานะ: แผนเท่านั้น ยังไม่ลงมือพัฒนา ตามคำสั่งล่าสุดของผู้ใช้

## เป้าหมาย

1. แก้ไฟล์เดิมโดยรักษาฟีเจอร์และเนื้อหาที่ไม่เกี่ยวกับคำสั่งใหม่
2. Continue ใช้ข้อสรุปของ AI และขั้นตอนค้าง ไม่ดึงข้อความร่างในช่องแชท
3. เปลี่ยนแผนเป็นรายการงานที่บันทึกถาวร ทำตามลำดับ และตรวจหลักฐานก่อนทำเครื่องหมายเสร็จ

## สิ่งที่พบจากโค้ด

- generateAndSaveBrowserDemo ขอให้โมเดลส่งเนื้อหาเต็มไฟล์ แล้วเขียนทับ revision ปัจจุบัน แม้เป็น follow-up
- requestBrowserAgentChat มี task context อยู่แล้ว แต่ context แบบจำกัดขนาดไม่รับประกันว่าไฟล์เป้าหมายฉบับเต็มจะถูกส่งทุกครั้ง
- continueBrowserAgentTask ล้าง plan/manifest/progress แล้ววางแผนใหม่ จึงไม่ใช่การ resume ขั้นตอนเดิม
- WebAiContinueTask อ่าน els.input.value สำหรับการทำต่อในบางสถานะ
- artifactProgress เก็บรายการไฟล์ ไม่ใช่ขั้นตอนจากแผนและเกณฑ์ยอมรับ
- Workspace มี revision store อยู่แล้ว ควรต่อยอดให้กู้คืนได้ ไม่สร้างระบบจัดเก็บซ้ำ

## ระยะ 1 — แก้ไฟล์อย่างรักษางานเดิม

- แยก create กับ edit ชัดเจน: ไฟล์ใหม่ส่งทั้งไฟล์ได้ ไฟล์เดิมขอ patch เฉพาะส่วน
- อ่านไฟล์เป้าหมายจาก IndexedDB โดยตรง พร้อม version/hash; ไม่พึ่ง context ที่อาจถูกตัด
- ใช้รูปแบบ edit ที่ตรวจได้ เช่น exact search/replace พร้อม base revision; ข้อความค้นหาต้องตรงตำแหน่งเดียว
- ตรวจ patch ทุกส่วนในหน่วยความจำก่อนเขียน ห้ามใช้ patch เพียงบางส่วนเมื่อส่วนอื่นผิด
- ถ้าไฟล์ใหญ่เกินงบ context ให้แบ่งอ่านเป็นส่วนที่ระบุตำแหน่งได้ ไม่ตัดไฟล์เงียบ ๆ แล้วขอเขียนใหม่
- ตรวจ expected revision และบันทึก revision ใหม่ใน transaction เดียว ป้องกันการแก้พร้อมกันจากผู้ใช้หรืออีกแท็บ
- การลบ/เขียนใหม่ทั้งไฟล์ต้องสัมพันธ์กับคำสั่งที่ต้องการจริง ไม่ใช้เป็น fallback ของ patch ที่ผิด
- เก็บ revision ก่อนแก้และแสดง diff/ปุ่มคืนรุ่นก่อนหน้า; การ rollback ต้องตรวจว่าไม่มีงานใหม่ถูกเขียนตามมา
- ตรวจฟีเจอร์เดิมที่สำคัญหลังแก้ เพราะ patch ที่ถูก syntax ยังอาจเปลี่ยนพฤติกรรมผิดได้

เกณฑ์ผ่าน: เพิ่มฟีเจอร์ใน Todo เดิมแล้วฟีเจอร์เดิมยังอยู่; patch ผิด/ถูกตัด/ไฟล์เปลี่ยนระหว่างรอ ต้องไม่เขียนทับ; คืน revision ก่อนหน้าได้

## ระยะ 2 — Continue จากสถานะและข้อสรุปของ AI

- บันทึก handoff แบบมีโครงสร้าง: summary, completedSteps, nextStepId, remainingWork, blockers, artifactRevisions
- เก็บ handoff ที่ผูกกับ taskId และ planVersion; ตรวจเทียบกับไฟล์จริงก่อนใช้
- แยกการส่งคำสั่งใหม่ออกจาก Continue: ปุ่มส่งใช้ข้อความผู้ใช้ ส่วน Continue ใช้ handoff และ TODO
- Continue ไม่อ่านหรือล้างข้อความร่างในช่องแชท
- ลำดับการทำต่อ: resume ขั้นค้าง → แก้ปัญหาขั้นที่ล้มเหลว → Preview/ตรวจผล → ขั้นถัดไป
- เมื่อทุกขั้นเสร็จแล้ว ให้แสดงว่าเสร็จ ไม่สร้างงานซ้ำจากแผนเก่า
- งานเก่าที่ยังไม่มี handoff ให้สร้างจากแผน เหตุการณ์ และ revision ที่ตรวจพบ โดยไม่กล่าวอ้างว่าทำขั้นใดสำเร็จหากไม่มีหลักฐาน

เกณฑ์ผ่าน: พิมพ์ข้อความร่างแล้วกด Continue ต้องทำตามงานค้างของ AI และร่างยังอยู่; reload แล้วทำขั้นเดิมต่อได้; กดซ้ำไม่เริ่มงานซ้อน

## ระยะ 3 — แผน → TODO → ทำทีละขั้น

- สร้าง schema ของแผน: planVersion และ steps ที่มี id, title, dependencies, targetFiles, acceptance, status, evidence
- สถานะขั้น: pending, running, blocked, failed, done
- บันทึกข้อมูลหลักใน IndexedDB; แสดง TODO.md และรายการในหน้า Workspace จากข้อมูลชุดเดียว ไม่แยก state สองชุด
- ให้ AI แตกแผนเป็นขั้นย่อยตามผลลัพธ์ที่ต้องการ ไม่ถือว่าหนึ่งไฟล์เท่ากับหนึ่งงานเสมอไป
- ตรวจ schema, path, dependency ที่มีจริง และวงจร dependency ก่อนเริ่ม
- ตัวรันเลือกขั้น pending ที่ dependencies เสร็จแล้ว ทำครั้งละหนึ่งขั้น
- ส่งเฉพาะเป้าหมายของขั้น บริบทที่จำเป็น ไฟล์เดิม และข้อกำหนดรักษาฟีเจอร์ให้โมเดลเล็ก
- แต่ละขั้นต้องมีหลักฐานการบันทึก/readback และเกณฑ์ยอมรับที่เกี่ยวข้อง ก่อนเปลี่ยนเป็น done
- ขั้นที่ต้องใช้หลายไฟล์ให้ตรวจครบชุดและ commit อย่างสอดคล้องกัน; ไม่แสดงผลครึ่งชุดเป็นงานสำเร็จ
- checkpoint หลังแต่ละขั้น; token limit หรือ network error ต้องรักษางานที่เสร็จแล้ว
- reload ขณะ running ให้ตรวจ checkpoint ก่อน retry; ใช้ step/run ID ป้องกันการทำซ้ำ
- งานใหม่จากผู้ใช้สร้าง plan version ใหม่ พร้อมรักษาประวัติและสถานะขั้นที่ยังใช้ได้
- เมื่อจบงานเว็บ เปิดและรัน Preview อัตโนมัติ และบันทึกผลตรวจแยกจากสถานะบันทึกไฟล์

เกณฑ์ผ่าน: แผนหลายขั้นแสดงรายการจริง ทำตาม dependencies ติ๊กเสร็จตามหลักฐาน และทำต่อจากขั้นค้างได้โดยไม่ย้อนเขียนงานที่เสร็จแล้ว

## ลำดับงานและจุดเชื่อม

1. ออกแบบ schema ของ patch, handoff, plan/checkpoint และ migration งานเก่า
2. ทำ revision guard/atomic write/rollback ใน workspace.js
3. ทำ edit pipeline ใน app-core.js และทดสอบการรักษาฟีเจอร์เดิม
4. ทำ Continue + handoff โดยแยกจากช่องแชท
5. ทำ step executor + TODO UI ใน app-core.js และ main-ui-v2.js
6. เชื่อม Preview/verification และทดสอบครบวงจร

ถ้ากระจายงานภายหลัง: ทีมจัดเก็บรับข้อ 2, ทีม runtime รับข้อ 3–5 หลังตกลง schema, ทีมทดสอบตรวจการชน revision/refresh/token limit และ acceptance ของผู้ใช้

## ชุดทดสอบก่อนเผยแพร่

- สร้าง Todo → เพิ่มตัวกรอง → เพิ่มแก้ชื่อ โดยฟีเจอร์ก่อนหน้ายังใช้ได้
- patch ไม่ตรง/ตรงหลายแห่ง/JSON ขาด/ผลตอบถูกตัด: revision เดิมไม่เปลี่ยน
- ผู้ใช้แก้ไฟล์ระหว่างรอ AI และอีกแท็บเขียนพร้อมกัน: ไม่เกิด lost update
- Continue ไม่ใช้ข้อความร่าง และไม่ทำขั้น done ซ้ำ
- แผนอย่างน้อยสามขั้น: dependency ถูกต้อง สถานะมีหลักฐาน ทำต่อได้หลัง reload และ quota หมด
- Preview ล้มเหลวต้องไม่รายงานงานเสร็จทั้งหมด; แก้แล้วรันใหม่อัตโนมัติได้
- งานเอกสารยังบันทึกและเปิดอ่านได้โดยไม่บังคับ HTML Preview

ข้อจำกัด: ยังไม่ได้ทดสอบกับโมเดลจริงในแผนนี้ ต้องตรวจความแม่นยำของ patch และการแตกขั้นกับ OpenTyphoon ก่อนเผยแพร่
