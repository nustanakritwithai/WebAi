# แผน WebAi: โมเดลเล็กที่ทำงานเป็น Agent ครบวงจร

สถานะ: แผนสำหรับพัฒนาต่อ ไม่ใช่รายงานว่าฟีเจอร์ทั้งหมดทำเสร็จแล้ว

## เป้าหมายและขอบเขต

ผู้ใช้สั่งสร้างเว็บหรือเอกสารแล้วได้รับไฟล์ในโฟลเดอร์งาน เปิดดูผลและสั่งแก้ต่อได้ ระบบตรวจหลักฐานก่อนรายงานความสำเร็จ โมเดลเล็กทำหน้าที่วางแผนและเสนอการแก้ไข ส่วนโปรแกรมควบคุมสถานะ เครื่องมือ การบันทึก และการตรวจผล

Browser Agent เป็นเส้นทางหลัก ใช้ IndexedDB เก็บงานและ iframe แสดงผล ใช้ proxy เดิมเรียกโมเดลโดยเก็บ API key ฝั่งเซิร์ฟเวอร์ Core สำหรับงาน native เป็นความสามารถเสริม ไม่เป็นเงื่อนไขสำหรับสร้างเว็บหรือเอกสารใน browser

แผนนี้ต่อยอดจาก DEVELOPMENT_PLAN_V05.md ซึ่งเน้น Core/Native Worker โดยไม่ถือว่าความสามารถฝั่ง Core มีอยู่ใน browser โดยอัตโนมัติ รอบแรกไม่รวมการฝึกโมเดลใหม่ การรัน shell ใน browser หรือการสร้าง DOCX/PDF

## สิ่งที่ต้องตรวจยืนยันก่อนเริ่ม

- เส้นทาง Auto, Agent และ UI ใหม่เรียกตัวควบคุมเดียวกันจริงหรือไม่
- การสร้างไฟล์หลังอนุมัติ การโหลดงานคืน และการแก้ต่อใช้ task ID เดียวกันหรือไม่
- Preview และ Verify ตรวจไฟล์ revision เดียวกันหรือไม่ โดยเฉพาะหลังผู้ใช้แก้ไฟล์เอง
- Memory ส่งบริบทตรงกับไฟล์ปัจจุบันหรือยังอ้างผลลัพธ์เก่า
- ตรวจบน browser จริงด้วย provider จำลองและ provider จริงแยกกัน; CI ที่ตรวจข้อความในซอร์สไม่เพียงพอ

## โครงสร้างการทำงานเป้าหมาย

คำสั่ง → เลือกงานเดิม/งานใหม่ → อ่านไฟล์และบริบท → วางแผน → เรียกเครื่องมือเขียนไฟล์ → ตรวจผล → แก้ตามข้อผิดพลาด → ส่งมอบ

ใช้ task record เดียวประกอบด้วย id, goal, status, workspaceFolder, iteration, budget, file revisions และ verification evidence ทุกหน้าจออ่านสถานะจาก record นี้ แชทแสดงคำอธิบายและรายชื่อไฟล์ ส่วนเนื้อหางานอ่านจาก Workspace

ไฟล์อยู่ภายใต้ tasks/<task-id>/ เช่น PLAN.md, README.md, index.html, style.css และ app.js งาน Agent/Auto สร้าง artifact หลังแผนผ่าน validation โดยอัตโนมัติ; แชทแสดงสรุปและรายชื่อไฟล์ ไม่แสดง source code เป็นผลลัพธ์หลัก งานเอกสารอย่างเดียวต้องสร้าง Markdown ได้โดยไม่บังคับให้มี HTML/CSS/JS ปุ่มงานใหม่แยกจากคำสั่งแก้ต่อและไม่ลบไฟล์งานเก่า

สถานะหลัก: idle → planning → awaiting_approval → executing → verifying → completed โดยมี repairing, failed, cancelled และ interrupted สำหรับกรณีที่ต้องจัดการเพิ่ม การรีเฟรชระหว่างเขียนไฟล์ต้องตรวจ transaction ก่อนเริ่มต่อ ไม่เรียกโมเดลหรือเขียนซ้ำทันที

## ลำดับพัฒนาและเกณฑ์ผ่าน

| ระยะ | งานที่ต้องทำ | หลักฐานที่ต้องผ่าน |
| --- | --- | --- |
| 1: วงจรงานเดียว | รวม Auto/Agent, แยกงานใหม่กับแก้ต่อ, กู้ task เดิม | สั่งสร้างแล้วแก้ต่อยังใช้โฟลเดอร์เดิม; refresh คืนสถานะถูกต้อง; UI ไม่รันซ้ำ |
| 2: เครื่องมือไฟล์ | list/read/write/patch ผ่าน schema, เขียนหลายไฟล์เป็น transaction, ตรวจ revision ก่อนทับ | บันทึกแล้วอ่านกลับตรงกัน; ปฏิเสธ patch เก่า; quota error ไม่ทำให้งานเสียครึ่งชุด; เอกสารล้วนทำได้ |
| 3: บริบทสำหรับโมเดลเล็ก | เลือกไฟล์เกี่ยวข้อง, สรุปงาน, แนบ error ที่จำเป็น, คุม token | โมเดลเห็นไฟล์ที่แก้ล่าสุด; บริบทไม่เกินงบ; สรุปไม่แทนที่ไฟล์ต้นฉบับ |
| 4: Preview/Verify | ใช้ไฟล์จาก Workspace, ผูกผลกับ task/revision, ตรวจ load/runtime และพฤติกรรม | แก้ไฟล์หลัง preview แล้วผลเก่าใช้ไม่ได้; runtime error ต้องไม่ผ่าน; To-do เพิ่ม/ติ๊ก/ลบได้จริง |
| 5: วงจรซ่อม | ส่ง error กลับให้โมเดลเสนอ patch, จำกัดรอบ/งบ, หยุดและยกเลิกได้ | แก้บั๊กตัวอย่างสำเร็จ; error ซ้ำหรือหมดงบหยุดพร้อมสาเหตุ; cancel ไม่รับผลเก่ามาเขียนทับ |
| 6: ส่งมอบ | เปิดงานเก่า, ประวัติ revision, export/import โฟลเดอร์, คู่มือสั้น | export/import ได้เนื้อหาเดิม; สั่งแก้ต่องานที่เปิดใหม่ได้; แสดงสิ่งที่ตรวจแล้วและยังไม่ตรวจ |

เริ่มระยะ 1–2 ก่อน แล้วจึงเพิ่มบริบทและวงจรซ่อม ไม่ขยาย autonomy ขณะการบันทึก/กู้สถานะยังไม่ผ่าน

## การใช้โมเดลเล็ก

แบ่งคำขอเป็นขั้นสั้น: เลือกไฟล์ → เสนอแผน → เสนอการแก้ → รับผลเครื่องมือ หลีกเลี่ยงให้โมเดลสร้างทั้งโปรเจกต์ใหม่ทุกครั้ง ส่ง schema เครื่องมือเฉพาะที่ใช้ในขั้นนั้น และให้โปรแกรมตรวจ args ก่อนทำงาน

ค่าตั้งต้นสำหรับทดลอง: ซ่อมได้ไม่เกิน 3 รอบ, แก้รูปแบบคำตอบผิด schema ได้ 1 ครั้ง, หยุดเมื่อข้อผิดพลาดเดิมซ้ำ 2 ครั้ง กำหนดงบ input/output รวมและ timeout ตามขนาด context/ข้อจำกัด provider ที่ตรวจจริง ค่าเหล่านี้เป็นข้อเสนอและต้องปรับจากผลวัด

บันทึก token จริงเมื่อ provider ส่ง usage; ถ้าไม่มีให้ระบุว่าเป็นค่าประมาณ Cache ต้องรวม task revision, model และเวอร์ชันคำสั่ง เพื่อไม่คืนคำตอบที่อ้างไฟล์เก่า

## ความถูกต้องและขอบเขตเครื่องมือ

- โปรแกรมกำหนด task folder และตรวจ path ไม่ใช้ path จากโมเดลโดยตรง
- เก็บ revision ก่อนเขียนและใช้ transaction สำหรับ artifact ชุดเดียวกัน
- แยกคำสั่งผู้ใช้ออกจากเนื้อหาไฟล์/คำตอบเครื่องมือ ไม่ให้ข้อความในไฟล์เพิ่มสิทธิ์เอง
- Preview แยก origin ด้วย sandbox และบล็อก network ตามนโยบายปัจจุบัน
- iframe load หรือรายงานจากโค้ดที่โมเดลสร้างไม่ใช่หลักฐานว่าคุณสมบัติทั้งหมดถูกต้อง ต้องมี behavioral checks จากตัวทดสอบภายนอกเมื่อจะอ้างผลนั้น
- การจำกัดเวลา iframe อย่างเดียวไม่รับประกันหยุด JavaScript infinite loop ต้องทดลอง isolation และระบุข้อจำกัดก่อนเปิดการซ่อมอัตโนมัติ
- โฟลเดอร์ browser ไม่ใช่ไฟล์บนเครื่อง; แสดงสถานะบันทึกและมี export เพื่อสำรองข้อมูล

## ชุดงานวัดผล

ใช้ชุดเริ่มต้นอย่างน้อย 20 งาน ครอบคลุมสร้างเว็บ, แก้เว็บเดิม, เอกสารล้วน, หลายไฟล์, provider timeout, malformed output, quota เต็ม, refresh ระหว่างงาน, ยกเลิก และไฟล์เปลี่ยนหลังตรวจ

วัดอัตรางานผ่านตาม acceptance criteria, จำนวนรอบซ่อม, token, เวลา, อัตรากู้สถานะ และการรายงานสำเร็จผิด เปรียบเทียบโมเดลด้วยโจทย์และงบเดียวกัน เป้าหมายเบื้องต้นคือผ่านอย่างน้อย 80% ของชุดงานและไม่มี false success ในกรณีเสียที่กำหนด ตัวเลขนี้เป็นเป้าหมาย ไม่ใช่ผลปัจจุบัน

ก่อนปล่อยแต่ละระยะต้องมี behavioral tests ที่เกี่ยวข้อง, CI ผ่าน, ทดลองใน browser และตรวจว่า deployment ใช้ commit ใหม่จริง รายงานแยกให้ชัดระหว่างเขียนโค้ดเสร็จ, ทดสอบผ่าน, merge แล้ว และทดสอบเว็บเผยแพร่แล้ว

## ผลลัพธ์ที่ต้องได้จากรอบแรก

ผู้ใช้สร้างงานหนึ่งครั้ง ได้ PLAN.md และไฟล์งานในโฟลเดอร์ กดดูผล สั่งแก้ต่อโดยไม่ต้องคัดลอกโค้ด และรีเฟรชกลับมาทำต่อได้ โดยทุกขั้นอ้าง task และ revision ชุดเดียวกัน
